enum AuthStatus {
  NOT_DETERMINED,
  NOT_LOGGED_IN,
  LOGGED_IN,
}

enum SortUser {
  Verified,
  Alphabetically,
  Newest,
  Oldest,
  MaxFollower,
}

enum SortList {
  Alphabetically,
  Newest,
  Oldest,
  ItemCount,
}

enum NotificationType { NOT_DETERMINED, Follow, LikeItem, LikeList, Mention }
